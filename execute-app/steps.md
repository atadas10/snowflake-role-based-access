1. PowerShell to start the Streamlit app:
    C:/Users/atadas/Downloads/Eligibility/.venv/Scripts/python.exe -m streamlit run app/main.py

2. Alternatively, in VS Code, you can:

    Open Command Palette (Ctrl+Shift+P)
    Type: Python: Select Interpreter
    Choose the one that shows .venv or /path/to/.venv/Scripts/python.exe
    Open a new terminal in VS Code (Ctrl+`)
    It will automatically activate the virtual environment
    
Once activated, you'll see (.venv) at the start of your terminal prompt. Then run: